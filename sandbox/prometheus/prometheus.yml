global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'temporal-clusters'
    static_configs:
      - targets:
        - 'temporal-cluster-1:8000'
        - 'temporal-cluster-2:8001'
        labels:
          group: 'temporal'
